<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <data>

        <!-- LIST VIEW -->
        <record id="view_op_activity_list" model="ir.ui.view">
            <field name="name">op.activity.list</field>
            <field name="model">op.activity</field>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">

                <list string="Activity Log">

                    <field name="student_id"/>
                    <field name="faculty_id"/>
                    <field name="type_id"/>
                    <field name="date"/>
                    <field name="description"/>

                </list>

            </field>
        </record>

        <!-- FORM VIEW -->
        <record id="view_op_activity_form" model="ir.ui.view">
            <field name="name">op.activity.form</field>
            <field name="model">op.activity</field>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">

                <form string="Activity Log">

                    <sheet>

                        <field name="active" invisible="1"/>

                        <widget name="web_ribbon"
                                title="Archived"
                                bg_color="bg-danger"
                                invisible="active"/>

                        <group>

                            <group>
                                <field name="student_id"/>
                                <field name="faculty_id"/>
                            </group>

                            <group>
                                <field name="type_id"/>
                                <field name="date"/>
                            </group>

                        </group>

                        <group>
                            <field name="description"/>
                        </group>

                    </sheet>

                    <chatter>
                        <field name="message_follower_ids" widget="mail_followers"/>
                        <field name="message_ids" widget="mail_thread"/>
                    </chatter>

                </form>

            </field>
        </record>

        <!-- SEARCH VIEW -->
        <record id="view_op_activity_search" model="ir.ui.view">
            <field name="name">op.activity.search</field>
            <field name="model">op.activity</field>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">

                <search string="Activity Log">

                    <field name="student_id"/>
                    <field name="faculty_id"/>
                    <field name="type_id"/>
                    <field name="date"/>

                    <filter string="Archived"
                            name="inactive"
                            domain="[('active', '=', False)]"/>

                </search>

            </field>
        </record>

        <!-- PIVOT VIEW -->
        <record id="view_op_activity_pivot" model="ir.ui.view">
            <field name="name">op.activity.pivot</field>
            <field name="model">op.activity</field>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">

                <pivot string="Activity Pivot">
                    <field name="student_id" type="row"/>
                    <field name="type_id" type="col"/>
                </pivot>

            </field>
        </record>

        <!-- GRAPH VIEW -->
        <record id="view_op_activity_graph" model="ir.ui.view">
            <field name="name">op.activity.graph</field>
            <field name="model">op.activity</field>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">

                <graph string="Activity Graph View" type="bar">
                    <field name="student_id"/>
                    <field name="type_id"/>
                </graph>

            </field>
        </record>

        <!-- ACTION -->
        <record id="act_open_op_activity_view" model="ir.actions.act_window">
            <field name="name">Activity Logs</field>
            <field name="res_model">op.activity</field>
            <field name="view_mode">list,form,pivot,graph</field>
            <field name="search_view_id" ref="view_op_activity_search"/>
        </record>

        <!-- FORM VIEW ACTION -->
        <record id="act_open_op_activity_view_form"
                model="ir.actions.act_window.view">
            <field name="act_window_id" ref="act_open_op_activity_view"/>
            <field name="sequence" eval="20"/>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_op_activity_form"/>
        </record>

        <!-- LIST VIEW ACTION -->
        <record id="act_open_op_activity_view_list"
                model="ir.actions.act_window.view">
            <field name="act_window_id" ref="act_open_op_activity_view"/>
            <field name="sequence" eval="10"/>
            <field name="view_mode">list</field>
            <field name="view_id" ref="view_op_activity_list"/>
        </record>

    </data>

</odoo>