<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Client Action للوحة تحكم العقارات -->
    <record id="action_eds_property_theme_dashboard" model="ir.actions.client">
        <field name="name">لوحة التحكم — العقارات</field>
        <field name="tag">eds_property_theme_dashboard</field>
        <field name="context">{}</field>
    </record>

</odoo>
