<?xml version='1.0' encoding='UTF-8'?>
<odoo>
    <data>
        <record id="view_op_exam_tree" model="ir.ui.view">
            <field name="name">op.exam.list</field>
            <field name="model">op.exam</field>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">
                <list string="Exam">
                    <field name="session_id"/>
                    <field name="name"/>
                    <field name="subject_id"/>
                    <field name="exam_code"/>
                    <field name="start_time"/>
                    <field name="end_time"/>
                    <field name="state"/>
                    <field name="note" optional="hide"/>
                    <field name="total_marks" optional="show"/>
                    <field name="min_marks" optional="show"/>
                </list>
            </field>
        </record>

        <record id="view_op_exam_form" model="ir.ui.view">
            <field name="name">op.exam.form</field>
            <field name="model">op.exam</field>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">
                <form string="Exam">
                    <header groups="base.group_user">
                        <button name="%(act_open_op_room_distribution_view)d" type="action" string="Schedule Exam" class="oe_highlight" invisible="state != 'draft'"/>
                        <button name="%(act_open_op_held_exam_view)d" type="action" string="Held Exam" class="oe_highlight" invisible="state != 'schedule'"/>
                        <button name="act_done" type="object" string="Done" class="oe_highlight" invisible="state != 'result_updated'"/>
                        <button name="act_draft" type="object" string="Draft" class="oe_highlight" invisible="state != 'cancel'"/>
                        <button string="Result Updated" class="oe_highlight" type="object" name="act_result_updated" invisible="state != 'held'"/>
                        <button name="act_cancel" type="object" string="Cancel" invisible="state not in ['draft', 'held', 'schedule']"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,schedule,held,result_updated,done"/>
                    </header>
                    <sheet>
                        <field name="active" invisible="1"/>
                        <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                        <group>
                            <group>
                                <field name="session_id" required="1"/>
                                <field name="subject_id"/>
                                <field name="start_time"/>
                                <field name="total_marks"/>
                            </group>
                            <group>
                                <field name="name"/>
                                <field name="exam_code"/>
                                <field name="end_time"/>
                                <field name="min_marks"/>
                            </group>
                        </group>
                        <group string="Attendees">
                            <field name="attendees_line" colspan="2" nolabel="1"/>
                        </group>
                        <separator colspan="2" string="Description"/>
                        <field name="note" colspan="2"/>
                    </sheet>
                    <chatter/>
                </form>
            </field>
        </record>

        <record id="view_op_exam_search" model="ir.ui.view">
            <field name="name">op.exam.search</field>
            <field name="model">op.exam</field>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">
                <search string="Exam">
                    <field name="session_id"/>
                    <field name="name"/>
                    <field name="subject_id"/>
                    <field name="exam_code"/>
                    <field name="start_time"/>
                    <field name="end_time"/>
                    <field name="state"/>
                    <field name="note"/>
                    <field name="total_marks"/>
                    <field name="min_marks"/>
                    <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                    </search>
            </field>
        </record>

        <record model="ir.actions.act_window" id="act_open_op_exam_view">
            <field name="name">Exams</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">op.exam</field>
            <field name="binding_view_types">form</field>
            <field name="view_mode">list,form</field>
            <field name="search_view_id" ref="view_op_exam_search"/>
            <field name="domain">[]</field>
            <field name="context">{}</field>
        </record>

        <record model="ir.actions.act_window.view" id="act_open_op_exam_view_form">
            <field name="act_window_id" ref="act_open_op_exam_view"/>
            <field name="sequence" eval="20"/>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_op_exam_form"/>
        </record>

        <record model="ir.actions.act_window.view" id="act_open_op_exam_view_tree">
            <field name="act_window_id" ref="act_open_op_exam_view"/>
            <field name="sequence" eval="10"/>
            <field name="view_mode">list</field>
            <field name="view_id" ref="view_op_exam_tree"/>
        </record>
    </data>
</odoo>
